select * from student where age > 10 and age < 20;
    select name from student;
select * from student where name like '%o%';
select * from student where age < student.name;
select * from student order by age;
create table student
(
    age  int,
    age  int,
    name int not null
);

